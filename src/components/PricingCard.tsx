'use client'; import { useState } from 'react'; interface PricingCardProps { plan: 'free' | 'premium'; price: string; features: string[]; onSelect: (plan: 'free' | 'premium') => void; isSelected?: boolean; } export default function PricingCard({ plan, price, features, onSelect, isSelected = false, }: PricingCardProps) { const [loading, setLoading] = useState(false); const handleClick = async () => { setLoading(true); try { onSelect(plan); } finally { setLoading(false); } }; return ( <div className={`border rounded-lg p-6 ${isSelected ? 'border-blue-600 bg-blue-50' : 'border-gray-200'}`}> <h3 className="text-xl font-bold mb-2 capitalize">{plan}</h3> <p className="text-3xl font-bold mb-4">{price}</p> <ul className="space-y-2 mb-6"> {features.map((feature, idx) => ( <li key={idx} className="flex items-center"> <span className="text-green-600 mr-2">✓</span> {feature} </li> ))} </ul> <button onClick={handleClick} disabled={loading} className={`w-full py-2 rounded-lg font-medium ${ isSelected ? 'bg-blue-600 text-white hover:bg-blue-700' : 'bg-gray-200 text-gray-800 hover:bg-gray-300' } disabled:opacity-50`} > {loading ? 'Processing...' : 'Select Plan'} </button> </div> ); }